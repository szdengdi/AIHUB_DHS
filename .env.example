# 开发模式内置插件源码根目录（仅 debug 构建生效，见 src-tauri 中
# service::plugin::preset::bundled_plugin_dir）
#
# 指向本地插件源码根目录：对 preset-plugins.json 中 internal 为 true 的每个插件，
# 运行时自动查找 <dir>/<id> —— 命中则以本地源码目录为 file: 安装目标
# （pnpm 目录联接 = 热更新：改源码 → 重启服务即生效，无需提交子插件 git、
# 无需跑 prebuild）；设置但缺该 id 则跳过（不回落随包目录）。
#
# 本文件为模板：复制为 .env 并填入你的路径（.env 已被 .gitignore 忽略）。
# 置空或删除本键 = 关闭覆盖，回落到随包分发的 resources/preset-plugins/<id>。
DEV_INTERNAL_PLUGINS_DIR=